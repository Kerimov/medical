# 📥 Импорт данных с продакшена в локальную БД

## Шаг 1: Получить DATABASE_URL продакшена

### Вариант 1: Из Vercel

1. Зайди в [Vercel Dashboard](https://vercel.com/dashboard)
2. Выбери проект
3. Перейди в **Settings** → **Environment Variables**
4. Найди `DATABASE_URL` и скопируй значение

### Вариант 2: Из Vercel CLI

```bash
vercel env pull .env.production
```

Затем скопируй `DATABASE_URL` из `.env.production`

## Шаг 2: Настроить переменные окружения

Добавь в `.env.local`:

```env
# DATABASE_URL для локальной БД (уже должен быть)
DATABASE_URL="postgresql://postgres:12345@localhost:5432/medical"

# DATABASE_URL для продакшена (для экспорта)
DATABASE_URL_PROD="postgresql://user:password@host:port/database"
```

**⚠️ ВАЖНО:** Замени `DATABASE_URL_PROD` на реальный URL продакшен БД!

## Шаг 3: Экспорт данных с продакшена

```bash
node scripts/export-production.js
```

Это создаст файл `export/production-data-YYYY-MM-DDTHH-MM-SS.json` со всеми данными.

## Шаг 4: Импорт в локальную БД

```bash
node scripts/import-production.js
```

Скрипт автоматически найдет последний файл экспорта и импортирует все данные.

## Что импортируется

- ✅ Пользователи (users)
- ✅ Профили врачей (doctorProfiles)
- ✅ Записи пациентов (patientRecords)
- ✅ Документы (documents)
- ✅ Анализы (analyses)
- ✅ Показатели (indicators)
- ✅ Напоминания (reminders)
- ✅ Рекомендации (recommendations)
- ✅ Записи на прием (appointments)
- ✅ Компании (companies)
- ✅ Продукты (products)
- ✅ И все остальные таблицы

## Проверка импорта

```bash
# Проверить количество записей
node scripts/check-users.js

# Или открыть Prisma Studio
npm run prisma:studio
```

## Важные замечания

1. **Пароли пользователей** импортируются как есть (хешированные)
2. **ID записей** сохраняются (upsert по ID)
3. **Даты** конвертируются из строк в Date объекты
4. **JSON поля** сохраняются как есть

## Если что-то пошло не так

```bash
# Очистить локальную БД (ОСТОРОЖНО - удалит все данные!)
npx prisma db push --force-reset

# Затем импортировать заново
node scripts/import-production.js
```

