# 🔍 Как найти таблицы в pgAdmin

## Таблицы существуют в базе данных!

Проверка показала, что в базе `medical` есть **22 таблицы**:
- Analysis
- Appointment
- Company
- Document
- User
- И другие...

## Почему таблицы не видны в pgAdmin?

### 1. Проверьте подключение к правильной базе данных

В pgAdmin:
1. Разверните сервер PostgreSQL
2. Разверните **Databases**
3. Убедитесь, что выбрана база данных **`medical`** (не `postgres`)

### 2. Проверьте схему `public`

Таблицы находятся в схеме `public`:

1. В pgAdmin разверните:
   ```
   Servers → PostgreSQL → Databases → medical → Schemas → public
   ```

2. Разверните **Tables** под схемой `public`

### 3. Обновите список (Refresh)

Если таблицы не видны:
1. Правой кнопкой на **Tables**
2. Выберите **Refresh**
3. Или нажмите **F5**

### 4. Проверьте фильтры

Убедитесь, что нет активных фильтров:
- В панели инструментов pgAdmin проверьте иконку фильтра
- Убедитесь, что фильтры отключены

## Быстрая проверка через SQL

В pgAdmin откройте Query Tool и выполните:

```sql
SELECT table_name 
FROM information_schema.tables 
WHERE table_schema = 'public' 
ORDER BY table_name;
```

Должно показать все 22 таблицы.

## Структура в pgAdmin должна быть:

```
Servers
└── PostgreSQL 17
    └── Databases
        └── medical ← ВОТ ЭТА БАЗА!
            └── Schemas
                └── public ← ВОТ ЭТА СХЕМА!
                    └── Tables ← ВОТ ЗДЕСЬ ДОЛЖНЫ БЫТЬ ТАБЛИЦЫ!
                        ├── Analysis
                        ├── Appointment
                        ├── Company
                        ├── Document
                        ├── User
                        └── ... (еще 17 таблиц)
```

## Если таблицы все еще не видны

Выполните в Query Tool:

```sql
-- Проверить все таблицы
SELECT * FROM information_schema.tables 
WHERE table_schema = 'public';

-- Проверить права доступа
SELECT grantee, privilege_type 
FROM information_schema.role_table_grants 
WHERE table_schema = 'public';
```

